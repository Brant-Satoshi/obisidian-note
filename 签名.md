在以太坊等区块链系统中：

- **每笔交易都附带签名（由私钥对交易哈希签名）**；
    
- 网络节点从交易数据中提取签名 `(r, s, v)`，对交易哈希执行 `ECDSA.recover()` 操作；
    
- 该操作会**恢复出签名者的公钥**；
    
- 进而可以推导出该公钥对应的地址 `address = last20(keccak256(pubkey))`；
    
- 节点据此判断：**这笔交易确实是你发的**（只你拥有私钥才可能签出这个交易）。
    

所以，交易发起者身份由**签名隐式绑定，无需提供用户名或公钥本身**。